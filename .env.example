# Generated/updated by `dfx deploy` / `dfx canister id`.
# Copy to `.env` for local development (do not commit `.env`).
DFX_VERSION='0.31.0'
DFX_NETWORK='local'

USER_NODE_CANISTER_ID=
CARDINAL_CANISTER_ID=
INTERNET_IDENTITY_CANISTER_ID=
OTHERLAND_CLIENT_CANISTER_ID=
